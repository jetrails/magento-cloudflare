<?php

	if ( $this->isValidAuth () && $this->isValidZone () ) {
		echo $this->renderSections ( "overview", array (
			"configuration",
			"status"
		));
	}
	else {
		echo $this->renderSections ( "overview", array (
			"configuration"
		));
	}

?>
